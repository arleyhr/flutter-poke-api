import 'package:flutter/material.dart';
import 'package:pokedex/ui/main_page.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MainPage();
  }
}
